extends layout

block content
    h1 Create a new chat room

    form(method="post")
        fieldset.form-group
            label(for="name") Name:
            input.form-control(name="name", type="text", placeholder="Name the room")
        button.btn.btn-primary(type="submit") Create new chat room
        a.btn.btn-default(href="/admin/rooms") Cancel